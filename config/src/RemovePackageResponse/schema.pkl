/// The schema of the request object for the add user endpoint ``
///
/// Returns the info about the yanked package
module RemovePackageResponse

extends "../RemovePackageByVersionResponse/schema.pkl"

version: String