/// The schema of the request object for the add user endpoint `api/user/{id}`
///
/// The object gets important information such as necessary information for registering the user
module AddUserRequest

