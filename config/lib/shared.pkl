import "pkl:semver"

typealias Version = String(semver.isValid(this))

/// The Version Control System
typealias VCS = "git"|"svn"|"fossil"|"mercurial"|"other"

/// The publishing status for the package
typealias PublishingStatus = "pending" | "error" | "success" | "idle" | "queue"