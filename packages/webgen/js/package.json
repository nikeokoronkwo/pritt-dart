{
    "name": "@pritt/webgen",
    "description": "Generate the web application for Pritt based on presets",
    "module": "index.js",
    "type": "module",
    "scripts": {
        "build": "esbuild index.ts --bundle --minify --format=esm --platform=browser --outfile=out/index.js"
    },
    "devDependencies": {
        "esbuild": "0.25.4",
        "typescript": "^5.8.3"
    },
    "dependencies": {
        "handlebars": "^4.7.8"
    }
}